#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; BorrowPool_AppContract.headers.fc
;;
;; Header files for AppContract
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_store_address_opt
builder __tact_store_address_opt(builder b, slice address) inline;

;; __tact_not_null
forall X -> X __tact_not_null(X x) impure inline;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_slice_eq_bits_nullable_right
int __tact_slice_eq_bits_nullable_right(slice a, slice b) inline;

;; __tact_dict_set_code
cell __tact_dict_set_code(cell dict, int id, cell code) inline;

;; __tact_dict_get_code
cell __tact_dict_get_code(cell dict, int id) inline;

;; $ChangeVal$_store
builder $ChangeVal$_store(builder build_0, (int, cell, slice) v) inline;

;; $ChangeVal$_store_cell
cell $ChangeVal$_store_cell((int, cell, slice) v, builder b) inline;

;; $Borrow$_store
builder $Borrow$_store(builder build_0, (int, int, int, int, slice, int, int, int) v) inline;

;; $Borrow$_store_cell
cell $Borrow$_store_cell((int, int, int, int, slice, int, int, int) v, builder b) inline;

;; $CheckTime$_store
builder $CheckTime$_store(builder build_0, tuple v) inline;

;; $CheckTime$_store_cell
cell $CheckTime$_store_cell(tuple v, builder b) inline;

;; $Update$_store
builder $Update$_store(builder build_0, (slice) v) inline;

;; $Update$_store_cell
cell $Update$_store_cell((slice) v, builder b) inline;

;; $UpdateEarn$_store
builder $UpdateEarn$_store(builder build_0, (slice) v) inline;

;; $UpdateEarn$_store_cell
cell $UpdateEarn$_store_cell((slice) v, builder b) inline;

;; $Repay$_store
builder $Repay$_store(builder build_0, (int) v) inline;

;; $Repay$_store_cell
cell $Repay$_store_cell((int) v, builder b) inline;

;; $Request$_store
builder $Request$_store(builder build_0, (int, slice) v) inline;

;; $Request$_store_cell
cell $Request$_store_cell((int, slice) v, builder b) inline;

;; $Approve$_store
builder $Approve$_store(builder build_0, (int, slice, slice, int) v) inline;

;; $Approve$_store_cell
cell $Approve$_store_cell((int, slice, slice, int) v, builder b) inline;

;; $Decline$_store
builder $Decline$_store(builder build_0, (int, slice, slice, int) v) inline;

;; $Decline$_store_cell
cell $Decline$_store_cell((int, slice, slice, int) v, builder b) inline;

;; $Withdraw$_store
builder $Withdraw$_store(builder build_0, (int, slice, int, slice) v) inline;

;; $Withdraw$_store_cell
cell $Withdraw$_store_cell((int, slice, int, slice) v, builder b) inline;

;; $InitCompensationPool$_store
builder $InitCompensationPool$_store(builder build_0, (slice, int) v) inline;

;; $InitCompensationPool$_store_cell
cell $InitCompensationPool$_store_cell((slice, int) v, builder b) inline;

;; $Deposit$_store
builder $Deposit$_store(builder build_0, (int, slice) v) inline;

;; $Deposit$_store_cell
cell $Deposit$_store_cell((int, slice) v, builder b) inline;

;; $DepositToComp$_store
builder $DepositToComp$_store(builder build_0, (int, slice) v) inline;

;; $DepositToComp$_store_cell
cell $DepositToComp$_store_cell((int, slice) v, builder b) inline;

;; $InitBorrowPool$_store
builder $InitBorrowPool$_store(builder build_0, (slice, int) v) inline;

;; $InitBorrowPool$_store_cell
cell $InitBorrowPool$_store_cell((slice, int) v, builder b) inline;

;; $AppContract$_store
builder $AppContract$_store(builder build_0, (int, int, int) v) inline;

;; $AppContract$_load
(slice, ((int, int, int))) $AppContract$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $BorrowContract$init$_store
builder $BorrowContract$init$_store(builder build_0, (slice, int, slice, int) v) inline;

;; $User$init$_store
builder $User$init$_store(builder build_0, (slice, slice) v) inline;

;; $BorrowPool$init$_store
builder $BorrowPool$init$_store(builder build_0, (int, slice) v) inline;

;; $CompensationPool$init$_store
builder $CompensationPool$init$_store(builder build_0, (int, slice) v) inline;

;; $AppContract$_contract_init
(int, int, int) $AppContract$_contract_init() impure inline;

;; $AppContract$_contract_load
(int, int, int) $AppContract$_contract_load() impure inline;

;; $AppContract$_contract_store
() $AppContract$_contract_store((int, int, int) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $global_newAddress
slice $global_newAddress(int $chain, int $hash) impure inline;

;; $global_contractAddressExt
slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline;

;; $global_contractAddress
slice $global_contractAddress((cell, cell) $s) impure inline;

;; $SendParameters$_constructor_to_value_code_data_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_code_data_body(slice $to, int $value, cell $code, cell $data, cell $body) inline;

;; $User$_init_child
(cell, cell) $User$_init_child(slice $addressUser, slice $app) inline_ref;

;; $global_login
slice $global_login(slice $address, slice $app, int $create) impure inline_ref;

;; $StateInit$_constructor_code_data
((cell, cell)) $StateInit$_constructor_code_data(cell $code, cell $data) inline;

;; $global_getWalletAddress
slice $global_getWalletAddress(int $publicKey) impure inline_ref;

;; $SendParameters$_constructor_to_value_mode_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline;

;; $ChangeVal$_constructor_val_newVal_initiator
((int, cell, slice)) $ChangeVal$_constructor_val_newVal_initiator(int $val, cell $newVal, slice $initiator) inline;

;; $BorrowContract$_init_child
(cell, cell) $BorrowContract$_init_child(slice $address, int $pool, slice $app, int $id) inline_ref;

;; $BorrowPool$_init_child
(cell, cell) $BorrowPool$_init_child(int $id, slice $app) inline_ref;

;; $CompensationPool$_init_child
(cell, cell) $CompensationPool$_init_child(int $id, slice $app) inline_ref;

;; $AppContract$_fun_usersData
((int, int, int), slice) $AppContract$_fun_usersData((int, int, int) $self, slice $address) impure inline_ref;

;; $AppContract$_fun_borrowPools
((int, int, int), slice) $AppContract$_fun_borrowPools((int, int, int) $self, int $id) impure inline_ref;

;; $AppContract$_fun_compPools
((int, int, int), slice) $AppContract$_fun_compPools((int, int, int) $self, int $id) impure inline_ref;

;; $AppContract$_fun_seqno
((int, int, int), int) $AppContract$_fun_seqno((int, int, int) $self) impure inline_ref;

;; $CheckTime$_constructor_
(tuple) $CheckTime$_constructor_() inline;

;; $Borrow$_constructor_idBorrow_idComp_amount_to_time_fcc_balanceComp_balanceBorrow
((int, int, int, int, slice, int, int, int)) $Borrow$_constructor_idBorrow_idComp_amount_to_time_fcc_balanceComp_balanceBorrow(int $idBorrow, int $idComp, int $amount, slice $to, int $time, int $fcc, int $balanceComp, int $balanceBorrow) inline;

;; $SendParameters$_constructor_to_value_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_body(slice $to, int $value, cell $body) inline;

;; $InitBorrowPool$_constructor_name_maxTime
((slice, int)) $InitBorrowPool$_constructor_name_maxTime(slice $name, int $maxTime) inline;

;; $UpdateEarn$_constructor_sender
((slice)) $UpdateEarn$_constructor_sender(slice $sender) inline;

;; $Withdraw$_constructor_from_from2_amount_to1
((int, slice, int, slice)) $Withdraw$_constructor_from_from2_amount_to1(int $from, slice $from2, int $amount, slice $to1) inline;

;; $Deposit$_constructor_id_sender
((int, slice)) $Deposit$_constructor_id_sender(int $id, slice $sender) inline;

;; $InitCompensationPool$_constructor_name_maxTime
((slice, int)) $InitCompensationPool$_constructor_name_maxTime(slice $name, int $maxTime) inline;

;; $Update$_constructor_sender
((slice)) $Update$_constructor_sender(slice $sender) inline;

;; $DepositToComp$_constructor_id_sender
((int, slice)) $DepositToComp$_constructor_id_sender(int $id, slice $sender) inline;

;; $Request$_constructor_id_user
((int, slice)) $Request$_constructor_id_user(int $id, slice $user) inline;

;; $Approve$_constructor_id_user_sender_balance
((int, slice, slice, int)) $Approve$_constructor_id_user_sender_balance(int $id, slice $user, slice $sender, int $balance) inline;

;; $Decline$_constructor_id_user_sender_balance
((int, slice, slice, int)) $Decline$_constructor_id_user_sender_balance(int $id, slice $user, slice $sender, int $balance) inline;

;; $Repay$_constructor_id
((int)) $Repay$_constructor_id(int $id) inline;


;; BorrowPool_AppContract.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(slice, slice) __tact_load_address_opt(slice cs) asm """

            b{00} SDBEGINSQ
            IF:<{
              PUSHNULL
            }>ELSE<{
              LDMSGADDR
              SWAP
            }>

""";

builder __tact_store_addr_none(builder b) asm "b{00} STSLICECONST";

builder __tact_store_address_opt(builder b, slice address) inline {
    if (null?(address)) {
        return __tact_store_addr_none(b);
    } else {
        return b.store_slice(address);
    }
}

forall X -> X __tact_not_null(X x) impure inline {
    throw_if(128, null?(x)); return x;
}

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

int __tact_slice_eq_bits_nullable_right(slice a, slice b) inline {
    return (null?(b)) ? (false) : (equal_slices_bits(a, b));
}

cell __tact_dict_set_code(cell dict, int id, cell code) inline {
    return udict_set_ref(dict, 16, id, code);
}

cell __tact_dict_get_code(cell dict, int id) inline {
    var (data, ok) = udict_get_ref?(dict, 16, id);
    throw_unless(135, ok);
    return data;
}

builder $global_beginCell() impure asm """
    NEWC
""";

cell $global_emptyCell() impure asm """
    <b b> PUSHREF // Pure Fift: "<b" creates a builder, "b>" turns it into a cell at compile time
""";

int $global_checkSignature(int $hash, slice $signature, int $publicKey) impure asm """
    CHKSIGNU
""";

int $global_contractHash(cell $code, cell $data) impure asm """
    s0 PUSH HASHCU // `data` hash
    s2 PUSH HASHCU // `code` hash
    SWAP2
    CDEPTH         // `data` depth
    SWAP
    CDEPTH         // `code` depth
    131380 INT     // (2 << 16) | (1 << 8) | 0x34

    // Group 2: Composition of the Builder
    NEWC
    24 STU  // store refs_descriptor | bits_descriptor | data
    16 STU  // store depth_descriptor for `code`
    16 STU  // store depth_descriptor for `data`
    256 STU // store `code` hash
    256 STU // store `data` hash

    // Group 3: SHA256 hash of the resulting Builder
    ONE HASHEXT_SHA256
""";

slice $Slice$_fun_asAddressUnsafe(slice $self) impure asm "NOP";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

slice $global_newAddress(int $chain, int $hash) impure inline {
    return $Slice$_fun_asAddressUnsafe($Cell$_fun_asSlice($Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 4, 3), $chain, 8), $hash, 256))));
}

slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline {
    int $hash = $global_contractHash($code, $data);
    return $global_newAddress($chain, $hash);
}

slice $global_contractAddress((cell, cell) $s) impure inline {
    var (($s'code, $s'data)) = $s;
    return $global_contractAddressExt(0, $s'code, $s'data);
}

slice $global_myAddress() impure asm """
    MYADDR
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

() $global_cashback(slice $to) impure asm """
    NEWC
    x{42_} STSLICECONST // .storeUint(0x10, 6)
    STSLICE          // .storeAddress(to)
    0 PUSHINT        // 0
    111 STUR         // .storeUint(0, 111)
                     // 4 zeros for coins and 107 zeros for lt, fees, etc.
    ENDC
    66 PUSHINT       // SendRemainingValue | SendIgnoreErrors
    SENDRAWMSG
""";

() $global_acceptMessage() impure asm """
    ACCEPT
""";

builder $Builder$_fun_storeCoins(builder $self, int $value) impure asm """
    STVARUINT16
""";

builder $Builder$_fun_storeAddress(builder $self, slice $address) impure asm """
    STSLICER
""";

(slice, int) $Slice$_fun_loadCoins(slice $self) impure asm( -> 1 0) """
    LDVARUINT16
""";

(slice, slice) $Slice$_fun_loadAddress(slice $self) impure asm( -> 1 0) """
    LDMSGADDR
""";

int $Cell$_fun_hash(cell $self) impure asm """
    HASHCU
""";

;; BorrowPool_AppContract.storage.fc
;;
;; Type: StateInit
;; TLB: _ code:^cell data:^cell = StateInit
;;

((cell, cell)) $StateInit$_constructor_code_data(cell $code, cell $data) inline {
    return ($code, $data);
}

;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_code_data_body(slice $to, int $value, cell $code, cell $data, cell $body) inline {
    return (0, $body, $code, $data, $value, $to, true);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, true);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_body(slice $to, int $value, cell $body) inline {
    return (0, $body, null(), null(), $value, $to, true);
}

;;
;; Type: ChangeVal
;; Header: 0xd02d0ac5
;; TLB: change_val#d02d0ac5 val:uint8 newVal:^cell initiator:address = ChangeVal
;;

builder $ChangeVal$_store(builder build_0, (int, cell, slice) v) inline {
    var (v'val, v'newVal, v'initiator) = v;
    build_0 = store_uint(build_0, 3492612805, 32);
    build_0 = build_0.store_uint(v'val, 8);
    build_0 = build_0.store_ref(v'newVal);
    build_0 = build_0.store_slice(v'initiator);
    return build_0;
}

cell $ChangeVal$_store_cell((int, cell, slice) v, builder b) inline {
    return $ChangeVal$_store(b, v).end_cell();
}

((int, cell, slice)) $ChangeVal$_constructor_val_newVal_initiator(int $val, cell $newVal, slice $initiator) inline {
    return ($val, $newVal, $initiator);
}

;;
;; Type: Borrow
;; Header: 0x1d37ca59
;; TLB: borrow#1d37ca59 idBorrow:uint64 idComp:uint64 amount:coins time:Maybe uint32 to:address fcc:coins balanceComp:coins balanceBorrow:coins = Borrow
;;

builder $Borrow$_store(builder build_0, (int, int, int, int, slice, int, int, int) v) inline {
    var (v'idBorrow, v'idComp, v'amount, v'time, v'to, v'fcc, v'balanceComp, v'balanceBorrow) = v;
    build_0 = store_uint(build_0, 490195545, 32);
    build_0 = build_0.store_uint(v'idBorrow, 64);
    build_0 = build_0.store_uint(v'idComp, 64);
    build_0 = build_0.store_varuint16(v'amount);
    build_0 = ~ null?(v'time) ? build_0.store_int(true, 1).store_uint(v'time, 32) : build_0.store_int(false, 1);
    build_0 = __tact_store_address_opt(build_0, v'to);
    build_0 = build_0.store_varuint16(v'fcc);
    build_0 = build_0.store_varuint16(v'balanceComp);
    build_0 = build_0.store_varuint16(v'balanceBorrow);
    return build_0;
}

cell $Borrow$_store_cell((int, int, int, int, slice, int, int, int) v, builder b) inline {
    return $Borrow$_store(b, v).end_cell();
}

((int, int, int, int, slice, int, int, int)) $Borrow$_constructor_idBorrow_idComp_amount_to_time_fcc_balanceComp_balanceBorrow(int $idBorrow, int $idComp, int $amount, slice $to, int $time, int $fcc, int $balanceComp, int $balanceBorrow) inline {
    return ($idBorrow, $idComp, $amount, $time, $to, $fcc, $balanceComp, $balanceBorrow);
}

;;
;; Type: CheckTime
;; Header: 0x0fd66c7c
;; TLB: check_time#0fd66c7c  = CheckTime
;;

builder $CheckTime$_store(builder build_0, tuple v) inline {
    build_0 = store_uint(build_0, 265710716, 32);
    return build_0;
}

cell $CheckTime$_store_cell(tuple v, builder b) inline {
    return $CheckTime$_store(b, v).end_cell();
}

(tuple) $CheckTime$_constructor_() inline {
    return empty_tuple();
}

;;
;; Type: Update
;; Header: 0x9044198f
;; TLB: update#9044198f sender:address = Update
;;

builder $Update$_store(builder build_0, (slice) v) inline {
    var (v'sender) = v;
    build_0 = store_uint(build_0, 2420382095, 32);
    build_0 = build_0.store_slice(v'sender);
    return build_0;
}

cell $Update$_store_cell((slice) v, builder b) inline {
    return $Update$_store(b, v).end_cell();
}

((slice)) $Update$_constructor_sender(slice $sender) inline {
    return ($sender);
}

;;
;; Type: UpdateEarn
;; Header: 0x22d38d52
;; TLB: update_earn#22d38d52 sender:address = UpdateEarn
;;

builder $UpdateEarn$_store(builder build_0, (slice) v) inline {
    var (v'sender) = v;
    build_0 = store_uint(build_0, 584289618, 32);
    build_0 = build_0.store_slice(v'sender);
    return build_0;
}

cell $UpdateEarn$_store_cell((slice) v, builder b) inline {
    return $UpdateEarn$_store(b, v).end_cell();
}

((slice)) $UpdateEarn$_constructor_sender(slice $sender) inline {
    return ($sender);
}

;;
;; Type: Repay
;; Header: 0x68939123
;; TLB: repay#68939123 id:uint256 = Repay
;;

builder $Repay$_store(builder build_0, (int) v) inline {
    var (v'id) = v;
    build_0 = store_uint(build_0, 1754501411, 32);
    build_0 = build_0.store_uint(v'id, 256);
    return build_0;
}

cell $Repay$_store_cell((int) v, builder b) inline {
    return $Repay$_store(b, v).end_cell();
}

((int)) $Repay$_constructor_id(int $id) inline {
    return ($id);
}

;;
;; Type: Request
;; Header: 0x52df2fba
;; TLB: request#52df2fba id:uint256 user:address = Request
;;

builder $Request$_store(builder build_0, (int, slice) v) inline {
    var (v'id, v'user) = v;
    build_0 = store_uint(build_0, 1390358458, 32);
    build_0 = build_0.store_uint(v'id, 256);
    build_0 = __tact_store_address_opt(build_0, v'user);
    return build_0;
}

cell $Request$_store_cell((int, slice) v, builder b) inline {
    return $Request$_store(b, v).end_cell();
}

((int, slice)) $Request$_constructor_id_user(int $id, slice $user) inline {
    return ($id, $user);
}

;;
;; Type: Approve
;; Header: 0x5f54bbeb
;; TLB: approve#5f54bbeb id:int257 user:address sender:address balance:coins = Approve
;;

builder $Approve$_store(builder build_0, (int, slice, slice, int) v) inline {
    var (v'id, v'user, v'sender, v'balance) = v;
    build_0 = store_uint(build_0, 1599388651, 32);
    build_0 = build_0.store_int(v'id, 257);
    build_0 = __tact_store_address_opt(build_0, v'user);
    build_0 = __tact_store_address_opt(build_0, v'sender);
    build_0 = build_0.store_varuint16(v'balance);
    return build_0;
}

cell $Approve$_store_cell((int, slice, slice, int) v, builder b) inline {
    return $Approve$_store(b, v).end_cell();
}

((int, slice, slice, int)) $Approve$_constructor_id_user_sender_balance(int $id, slice $user, slice $sender, int $balance) inline {
    return ($id, $user, $sender, $balance);
}

;;
;; Type: Decline
;; Header: 0x2cb63203
;; TLB: decline#2cb63203 id:int257 user:address sender:address balance:coins = Decline
;;

builder $Decline$_store(builder build_0, (int, slice, slice, int) v) inline {
    var (v'id, v'user, v'sender, v'balance) = v;
    build_0 = store_uint(build_0, 750137859, 32);
    build_0 = build_0.store_int(v'id, 257);
    build_0 = __tact_store_address_opt(build_0, v'user);
    build_0 = __tact_store_address_opt(build_0, v'sender);
    build_0 = build_0.store_varuint16(v'balance);
    return build_0;
}

cell $Decline$_store_cell((int, slice, slice, int) v, builder b) inline {
    return $Decline$_store(b, v).end_cell();
}

((int, slice, slice, int)) $Decline$_constructor_id_user_sender_balance(int $id, slice $user, slice $sender, int $balance) inline {
    return ($id, $user, $sender, $balance);
}

;;
;; Type: Withdraw
;; Header: 0x0abf5c5f
;; TLB: withdraw#0abf5c5f from:int257 from2:address amount:coins to1:address = Withdraw
;;

builder $Withdraw$_store(builder build_0, (int, slice, int, slice) v) inline {
    var (v'from, v'from2, v'amount, v'to1) = v;
    build_0 = store_uint(build_0, 180313183, 32);
    build_0 = build_0.store_int(v'from, 257);
    build_0 = __tact_store_address_opt(build_0, v'from2);
    build_0 = build_0.store_varuint16(v'amount);
    build_0 = __tact_store_address_opt(build_0, v'to1);
    return build_0;
}

cell $Withdraw$_store_cell((int, slice, int, slice) v, builder b) inline {
    return $Withdraw$_store(b, v).end_cell();
}

((int, slice, int, slice)) $Withdraw$_constructor_from_from2_amount_to1(int $from, slice $from2, int $amount, slice $to1) inline {
    return ($from, $from2, $amount, $to1);
}

;;
;; Type: InitCompensationPool
;; Header: 0x05ae90f1
;; TLB: init_compensation_pool#05ae90f1 name:^string maxTime:uint32 = InitCompensationPool
;;

builder $InitCompensationPool$_store(builder build_0, (slice, int) v) inline {
    var (v'name, v'maxTime) = v;
    build_0 = store_uint(build_0, 95326449, 32);
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'name));
    build_0 = build_0.store_uint(v'maxTime, 32);
    return build_0;
}

cell $InitCompensationPool$_store_cell((slice, int) v, builder b) inline {
    return $InitCompensationPool$_store(b, v).end_cell();
}

((slice, int)) $InitCompensationPool$_constructor_name_maxTime(slice $name, int $maxTime) inline {
    return ($name, $maxTime);
}

;;
;; Type: Deposit
;; Header: 0x1e4758c2
;; TLB: deposit#1e4758c2 id:uint32 sender:address = Deposit
;;

builder $Deposit$_store(builder build_0, (int, slice) v) inline {
    var (v'id, v'sender) = v;
    build_0 = store_uint(build_0, 507992258, 32);
    build_0 = build_0.store_uint(v'id, 32);
    build_0 = __tact_store_address_opt(build_0, v'sender);
    return build_0;
}

cell $Deposit$_store_cell((int, slice) v, builder b) inline {
    return $Deposit$_store(b, v).end_cell();
}

((int, slice)) $Deposit$_constructor_id_sender(int $id, slice $sender) inline {
    return ($id, $sender);
}

;;
;; Type: DepositToComp
;; Header: 0x8a6d35fc
;; TLB: deposit_to_comp#8a6d35fc id:uint32 sender:address = DepositToComp
;;

builder $DepositToComp$_store(builder build_0, (int, slice) v) inline {
    var (v'id, v'sender) = v;
    build_0 = store_uint(build_0, 2322413052, 32);
    build_0 = build_0.store_uint(v'id, 32);
    build_0 = __tact_store_address_opt(build_0, v'sender);
    return build_0;
}

cell $DepositToComp$_store_cell((int, slice) v, builder b) inline {
    return $DepositToComp$_store(b, v).end_cell();
}

((int, slice)) $DepositToComp$_constructor_id_sender(int $id, slice $sender) inline {
    return ($id, $sender);
}

;;
;; Type: InitBorrowPool
;; Header: 0x45273be3
;; TLB: init_borrow_pool#45273be3 name:^string maxTime:uint32 = InitBorrowPool
;;

builder $InitBorrowPool$_store(builder build_0, (slice, int) v) inline {
    var (v'name, v'maxTime) = v;
    build_0 = store_uint(build_0, 1160199139, 32);
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'name));
    build_0 = build_0.store_uint(v'maxTime, 32);
    return build_0;
}

cell $InitBorrowPool$_store_cell((slice, int) v, builder b) inline {
    return $InitBorrowPool$_store(b, v).end_cell();
}

((slice, int)) $InitBorrowPool$_constructor_name_maxTime(slice $name, int $maxTime) inline {
    return ($name, $maxTime);
}

;;
;; Type: BorrowContract
;; TLB: _ id:uint32 address:address createdTime:uint32 pool:uint256 sum:coins app:address endTime:uint64 = BorrowContract
;;

builder $BorrowContract$init$_store(builder build_0, (slice, int, slice, int) v) inline {
    var (v'address, v'pool, v'app, v'id) = v;
    build_0 = build_0.store_slice(v'address);
    build_0 = build_0.store_uint(v'pool, 256);
    build_0 = build_0.store_slice(v'app);
    var build_1 = begin_cell();
    build_1 = build_1.store_int(v'id, 257);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

(cell, cell) $BorrowContract$_init_child(slice $address, int $pool, slice $app, int $id) inline_ref {
    ;; Build init code cell

    slice sc' = __tact_child_contract_codes.begin_parse();
    cell source = sc'~load_dict();
    ;; Contract Code: BorrowContract
    cell init_code = __tact_dict_get_code(source, 55931);

    ;; Build init data cell
    cell contracts = new_dict();

    ;; Contract Code: CompensationPool
    cell code_21538 = __tact_dict_get_code(source, 21538);
    contracts = __tact_dict_set_code(contracts, 21538, code_21538);

    ;; Contract Code: RequestContract
    cell code_42506 = __tact_dict_get_code(source, 42506);
    contracts = __tact_dict_set_code(contracts, 42506, code_42506);

    ;; Contract Code: User
    cell code_57900 = __tact_dict_get_code(source, 57900);
    contracts = __tact_dict_set_code(contracts, 57900, code_57900);

    ;; Contract Code: BorrowPool
    cell code_33759 = __tact_dict_get_code(source, 33759);
    contracts = __tact_dict_set_code(contracts, 33759, code_33759);

    builder b = begin_cell();
    b = b.store_builder_ref(begin_cell().store_dict(contracts));
    b = b.store_int(false, 1);
    b = $BorrowContract$init$_store(b, ($address, $pool, $app, $id));
    return (init_code, b.end_cell());
}

;;
;; Type: User
;; TLB: _ addressUser:address rating:int257 app:address debts:dict<uint32, ^Debt{sum:coins,timetoreturn:uint32,borrowPool:uint256,compPool:uint256,commision:coins,fcc:coins}> investedIn:dict<address, ^CompPoolData{v:uint32,freezed:coins,address:address,balance:coins,earnCoff:uint64,freezeCoff:uint64,balanceUserAfter:coins}> compensationPools:dict<uint256, address> debtId:uint32 = User
;;

builder $User$init$_store(builder build_0, (slice, slice) v) inline {
    var (v'addressUser, v'app) = v;
    build_0 = build_0.store_slice(v'addressUser);
    build_0 = build_0.store_slice(v'app);
    return build_0;
}

(cell, cell) $User$_init_child(slice $addressUser, slice $app) inline_ref {
    ;; Build init code cell

    slice sc' = __tact_child_contract_codes.begin_parse();
    cell source = sc'~load_dict();
    ;; Contract Code: User
    cell init_code = __tact_dict_get_code(source, 57900);

    ;; Build init data cell
    cell contracts = new_dict();

    ;; Contract Code: CompensationPool
    cell code_21538 = __tact_dict_get_code(source, 21538);
    contracts = __tact_dict_set_code(contracts, 21538, code_21538);

    ;; Contract Code: RequestContract
    cell code_42506 = __tact_dict_get_code(source, 42506);
    contracts = __tact_dict_set_code(contracts, 42506, code_42506);

    ;; Contract Code: BorrowContract
    cell code_55931 = __tact_dict_get_code(source, 55931);
    contracts = __tact_dict_set_code(contracts, 55931, code_55931);

    ;; Contract Code: BorrowPool
    cell code_33759 = __tact_dict_get_code(source, 33759);
    contracts = __tact_dict_set_code(contracts, 33759, code_33759);

    builder b = begin_cell();
    b = b.store_builder_ref(begin_cell().store_dict(contracts));
    b = b.store_int(false, 1);
    b = $User$init$_store(b, ($addressUser, $app));
    return (init_code, b.end_cell());
}

;;
;; Type: BorrowPool
;; TLB: _ id:uint32 name:^string maxTime:uint32 app:address acc:coins v:uint32 borrowBalance:dict<int, coins> = BorrowPool
;;

builder $BorrowPool$init$_store(builder build_0, (int, slice) v) inline {
    var (v'id, v'app) = v;
    build_0 = build_0.store_int(v'id, 257);
    build_0 = build_0.store_slice(v'app);
    return build_0;
}

(cell, cell) $BorrowPool$_init_child(int $id, slice $app) inline_ref {
    ;; Build init code cell

    slice sc' = __tact_child_contract_codes.begin_parse();
    cell source = sc'~load_dict();
    ;; Contract Code: BorrowPool
    cell init_code = __tact_dict_get_code(source, 33759);

    ;; Build init data cell
    cell contracts = new_dict();

    ;; Contract Code: CompensationPool
    cell code_21538 = __tact_dict_get_code(source, 21538);
    contracts = __tact_dict_set_code(contracts, 21538, code_21538);

    ;; Contract Code: User
    cell code_57900 = __tact_dict_get_code(source, 57900);
    contracts = __tact_dict_set_code(contracts, 57900, code_57900);

    ;; Contract Code: RequestContract
    cell code_42506 = __tact_dict_get_code(source, 42506);
    contracts = __tact_dict_set_code(contracts, 42506, code_42506);

    ;; Contract Code: BorrowContract
    cell code_55931 = __tact_dict_get_code(source, 55931);
    contracts = __tact_dict_set_code(contracts, 55931, code_55931);

    builder b = begin_cell();
    b = b.store_builder_ref(begin_cell().store_dict(contracts));
    b = b.store_int(false, 1);
    b = $BorrowPool$init$_store(b, ($id, $app));
    return (init_code, b.end_cell());
}

;;
;; Type: CompensationPool
;; TLB: _ id:uint32 name:^string maxTime:uint32 app:address freezed:coins acc:uint32 v:uint32 fcc:coins borrowBalance:dict<int, coins> debtId:uint256 = CompensationPool
;;

builder $CompensationPool$init$_store(builder build_0, (int, slice) v) inline {
    var (v'id, v'app) = v;
    build_0 = build_0.store_int(v'id, 257);
    build_0 = build_0.store_slice(v'app);
    return build_0;
}

(cell, cell) $CompensationPool$_init_child(int $id, slice $app) inline_ref {
    ;; Build init code cell

    slice sc' = __tact_child_contract_codes.begin_parse();
    cell source = sc'~load_dict();
    ;; Contract Code: CompensationPool
    cell init_code = __tact_dict_get_code(source, 21538);

    ;; Build init data cell
    cell contracts = new_dict();

    ;; Contract Code: RequestContract
    cell code_42506 = __tact_dict_get_code(source, 42506);
    contracts = __tact_dict_set_code(contracts, 42506, code_42506);

    ;; Contract Code: BorrowPool
    cell code_33759 = __tact_dict_get_code(source, 33759);
    contracts = __tact_dict_set_code(contracts, 33759, code_33759);

    ;; Contract Code: User
    cell code_57900 = __tact_dict_get_code(source, 57900);
    contracts = __tact_dict_set_code(contracts, 57900, code_57900);

    ;; Contract Code: BorrowContract
    cell code_55931 = __tact_dict_get_code(source, 55931);
    contracts = __tact_dict_set_code(contracts, 55931, code_55931);

    builder b = begin_cell();
    b = b.store_builder_ref(begin_cell().store_dict(contracts));
    b = b.store_int(false, 1);
    b = $CompensationPool$init$_store(b, ($id, $app));
    return (init_code, b.end_cell());
}

;;
;; Type: AppContract
;; TLB: _ nowId:uint256 seqno:uint32 serverPublicKey:uint256 = AppContract
;;

builder $AppContract$_store(builder build_0, (int, int, int) v) inline {
    var (v'nowId, v'seqno, v'serverPublicKey) = v;
    build_0 = build_0.store_uint(v'nowId, 256);
    build_0 = build_0.store_uint(v'seqno, 32);
    build_0 = build_0.store_uint(v'serverPublicKey, 256);
    return build_0;
}

(slice, ((int, int, int))) $AppContract$_load(slice sc_0) inline {
    var v'nowId = sc_0~load_uint(256);
    var v'seqno = sc_0~load_uint(32);
    var v'serverPublicKey = sc_0~load_uint(256);
    return (sc_0, (v'nowId, v'seqno, v'serverPublicKey));
}

(int, int, int) $AppContract$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    __tact_child_contract_codes = $sc~load_ref();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$AppContract$_load();
    }
    else {
        return $AppContract$_contract_init();
    }
}

() $AppContract$_contract_store((int, int, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_ref(__tact_child_contract_codes);
    b = b.store_int(true, 1);
    b = $AppContract$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract AppContract functions
;;

(int, int, int) $AppContract$_contract_init() impure inline {
    var (($self'nowId, $self'seqno, $self'serverPublicKey)) = (1, 0, 34266914862605588507594995374584153816154521013833904446998141995886007185266);
    return ($self'nowId, $self'seqno, $self'serverPublicKey);
}

slice $global_login(slice $address, slice $app, int $create) impure inline_ref {
    var ($init'code, $init'data) = $User$_init_child($address, $app);
    slice $contractAddress = $global_contractAddress(($init'code, $init'data));
    if ((~ null?($create))) {
        $global_send($SendParameters$_constructor_to_value_code_data_body($contractAddress, 20000000, $init'code, $init'data, $global_emptyCell()));
        return $contractAddress;
    }
    return $contractAddress;
}

slice $global_getWalletAddress(int $publicKey) impure inline_ref {
    cell $code = $Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 791288306, 32), $publicKey, 256), 0, 1));
    var ($stateInit'code, $stateInit'data) = $StateInit$_constructor_code_data($code, $Builder$_fun_endCell($global_beginCell()));
    return $global_contractAddress(($stateInit'code, $stateInit'data));
}

((int, int, int), slice) $AppContract$_fun_usersData((int, int, int) $self, slice $address) impure inline_ref {
    var (($self'nowId, $self'seqno, $self'serverPublicKey)) = $self;
    var ($init'code, $init'data) = $User$_init_child($address, $global_myAddress());
    slice $addresscontract = $global_contractAddress(($init'code, $init'data));
    var $fresh$ret_212 = $addresscontract;
    return (($self'nowId, $self'seqno, $self'serverPublicKey), $fresh$ret_212);
}

((int, int, int), slice) $AppContract$_fun_borrowPools((int, int, int) $self, int $id) impure inline_ref {
    var (($self'nowId, $self'seqno, $self'serverPublicKey)) = $self;
    var ($init'code, $init'data) = $BorrowPool$_init_child($id, $global_myAddress());
    slice $childAddress = $global_contractAddress(($init'code, $init'data));
    var $fresh$ret_213 = $childAddress;
    return (($self'nowId, $self'seqno, $self'serverPublicKey), $fresh$ret_213);
}

((int, int, int), slice) $AppContract$_fun_compPools((int, int, int) $self, int $id) impure inline_ref {
    var (($self'nowId, $self'seqno, $self'serverPublicKey)) = $self;
    var ($init'code, $init'data) = $CompensationPool$_init_child($id, $global_myAddress());
    slice $childAddress = $global_contractAddress(($init'code, $init'data));
    var $fresh$ret_214 = $childAddress;
    return (($self'nowId, $self'seqno, $self'serverPublicKey), $fresh$ret_214);
}

((int, int, int), int) $AppContract$_fun_seqno((int, int, int) $self) impure inline_ref {
    var (($self'nowId, $self'seqno, $self'serverPublicKey)) = $self;
    var $fresh$ret_215 = $self'seqno;
    return (($self'nowId, $self'seqno, $self'serverPublicKey), $fresh$ret_215);
}

;;
;; Get methods of a Contract AppContract
;;

_ %usersData(slice $address) method_id(123767) {
    slice $address = $address;
    var self = $AppContract$_contract_load();
    var res = self~$AppContract$_fun_usersData($address);
    return res;
}

_ %borrowPools(int $id) method_id(105061) {
    int $id = $id;
    var self = $AppContract$_contract_load();
    var res = self~$AppContract$_fun_borrowPools($id);
    return res;
}

_ %compPools(int $id) method_id(111216) {
    int $id = $id;
    var self = $AppContract$_contract_load();
    var res = self~$AppContract$_fun_compPools($id);
    return res;
}

_ %seqno() method_id(85143) {
    var self = $AppContract$_contract_load();
    var res = self~$AppContract$_fun_seqno();
    return res;
}

;;
;; Routing of a Contract AppContract
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'nowId, $self'seqno, $self'serverPublicKey) = $AppContract$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive Time message
    if (op == 2793874247) {
        var $msg'address = in_msg~load_msg_addr();
        var $msg'id = in_msg~load_uint(16);
        var $msg'pool = in_msg~load_uint(16);
        slice $user = $global_login($msg'address, $global_myAddress(), null());
        var ($init'code, $init'data) = $BorrowContract$_init_child($user, $msg'pool, $global_myAddress(), $msg'id);
        slice $borrowcontractaddress = $global_contractAddress(($init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_mode_body($borrowcontractaddress, $Context$_get_value(__tact_context_get()), 2, $CheckTime$_store_cell($CheckTime$_constructor_(), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Login message
    if (op == 1592348451) {
        var $msg = empty_tuple();
        throw_unless(56619, ($Context$_get_value(__tact_context_get()) > 20000000));
        $global_login(__tact_context_get_sender(), $global_myAddress(), 1);
        $global_cashback(__tact_context_get_sender());
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Borrow message
    if (op == 490195545) {
        var $msg'idBorrow = in_msg~load_uint(64);
        var $msg'idComp = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_varuint16();
        var $msg'time = in_msg~load_int(1) ? in_msg~load_uint(32) : null();
        var $msg'to = in_msg~__tact_load_address_opt();
        var $msg'fcc = in_msg~load_varuint16();
        var $msg'balanceComp = in_msg~load_varuint16();
        var $msg'balanceBorrow = in_msg~load_varuint16();
        throw_unless(56619, ($Context$_get_value(__tact_context_get()) >= 10000000));
        var ($initComp'code, $initComp'data) = $CompensationPool$_init_child($msg'idComp, $global_myAddress());
        slice $comppoolAddress = $global_contractAddress(($initComp'code, $initComp'data));
        $global_send($SendParameters$_constructor_to_value_mode_body($comppoolAddress, 0, 64, $Borrow$_store_cell($Borrow$_constructor_idBorrow_idComp_amount_to_time_fcc_balanceComp_balanceBorrow($msg'idBorrow, $msg'idComp, $msg'amount, __tact_context_get_sender(), 0, $msg'fcc, $msg'balanceComp, $msg'balanceBorrow), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive CreateBorrowPool message
    if (op == 1366651737) {
        var $msg'name = in_msg~load_ref().begin_parse();
        var $msg'maxTime = in_msg~load_uint(32);
        int $id = $self'nowId;
        $self'nowId = ($self'nowId + 1);
        int $balance = $Context$_get_value(__tact_context_get());
        var ($init'code, $init'data) = $BorrowPool$_init_child($id, $global_myAddress());
        slice $borrowpoolAddress = $global_contractAddress(($init'code, $init'data));
        throw_unless(56619, (($balance - 30000000) > 0));
        slice $userAddress = $global_login(__tact_context_get_sender(), $global_myAddress(), null());
        cell $data = $Builder$_fun_endCell($Builder$_fun_storeCoins($Builder$_fun_storeAddress($global_beginCell(), $borrowpoolAddress), ($balance - 10000000)));
        $global_send($SendParameters$_constructor_to_value_body($userAddress, 10000000, $ChangeVal$_store_cell($ChangeVal$_constructor_val_newVal_initiator(3, $data, __tact_context_get_sender()), begin_cell())));
        $balance = ($balance - 10000000);
        $global_send($SendParameters$_constructor_to_value_code_data_body($borrowpoolAddress, $balance, $init'code, $init'data, $InitBorrowPool$_store_cell($InitBorrowPool$_constructor_name_maxTime($msg'name, $msg'maxTime), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Withdraw message
    if (op == 180313183) {
        var $msg'from = in_msg~load_int(257);
        var $msg'from2 = in_msg~__tact_load_address_opt();
        var $msg'amount = in_msg~load_varuint16();
        var $msg'to1 = in_msg~__tact_load_address_opt();
        throw_unless(41253, ($Context$_get_value(__tact_context_get()) >= 20000000));
        slice $userAddress = $global_login(__tact_context_get_sender(), $global_myAddress(), null());
        var ($init2'code, $init2'data) = $BorrowPool$_init_child($msg'from, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init2'code, $init2'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $UpdateEarn$_store_cell($UpdateEarn$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($userAddress, 10000000, $Withdraw$_store_cell($Withdraw$_constructor_from_from2_amount_to1($msg'from, $childAddress, $msg'amount, __tact_context_get_sender()), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Deposit message
    if (op == 507992258) {
        var $msg'id = in_msg~load_uint(32);
        var $msg'sender = in_msg~__tact_load_address_opt();
        var ($init'code, $init'data) = $BorrowPool$_init_child($msg'id, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $UpdateEarn$_store_cell($UpdateEarn$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        int $amountToSend = $Context$_get_value(__tact_context_get());
        throw_unless(28284, (($amountToSend - 20000000) > 0));
        int $balance = ($Context$_get_value(__tact_context_get()) - 10000000);
        $global_send($SendParameters$_constructor_to_value_body($childAddress, $balance, $Deposit$_store_cell($Deposit$_constructor_id_sender($msg'id, __tact_context_get_sender()), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive CreateCompensationPool message
    if (op == 2604694397) {
        var $msg'name = in_msg~load_ref().begin_parse();
        var $msg'maxTime = in_msg~load_uint(32);
        int $id = $self'nowId;
        $self'nowId = ($self'nowId + 1);
        int $balance = $Context$_get_value(__tact_context_get());
        var ($init'code, $init'data) = $CompensationPool$_init_child($id, $global_myAddress());
        slice $compensationpoolAddress = $global_contractAddress(($init'code, $init'data));
        throw_unless(56619, (($balance - 30000000) > 0));
        slice $userAddress = $global_login(__tact_context_get_sender(), $global_myAddress(), null());
        cell $data = $Builder$_fun_endCell($Builder$_fun_storeCoins($Builder$_fun_storeAddress($global_beginCell(), $compensationpoolAddress), ($balance - 10000000)));
        $global_send($SendParameters$_constructor_to_value_body($userAddress, 10000000, $ChangeVal$_store_cell($ChangeVal$_constructor_val_newVal_initiator(3, $data, __tact_context_get_sender()), begin_cell())));
        $balance = ($balance - 10000000);
        $global_send($SendParameters$_constructor_to_value_code_data_body($compensationpoolAddress, $balance, $init'code, $init'data, $InitCompensationPool$_store_cell($InitCompensationPool$_constructor_name_maxTime($msg'name, $msg'maxTime), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive WithdrawFromComp message
    if (op == 3292633415) {
        var $msg'from = in_msg~load_int(257);
        var $msg'from2 = in_msg~__tact_load_address_opt();
        var $msg'amount = in_msg~load_varuint16();
        var $msg'to1 = in_msg~__tact_load_address_opt();
        throw_unless(19281, ($Context$_get_value(__tact_context_get()) > 30000000));
        slice $userAddress = $global_login(__tact_context_get_sender(), $global_myAddress(), null());
        var ($init2'code, $init2'data) = $CompensationPool$_init_child($msg'from, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init2'code, $init2'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $Update$_store_cell($Update$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $UpdateEarn$_store_cell($UpdateEarn$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($userAddress, 10000000, $Withdraw$_store_cell($Withdraw$_constructor_from_from2_amount_to1($msg'from, $childAddress, $msg'amount, __tact_context_get_sender()), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive DepositToComp message
    if (op == 2322413052) {
        var $msg'id = in_msg~load_uint(32);
        var $msg'sender = in_msg~__tact_load_address_opt();
        var ($init'code, $init'data) = $CompensationPool$_init_child($msg'id, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $Update$_store_cell($Update$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $UpdateEarn$_store_cell($UpdateEarn$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        int $amountToSend = $Context$_get_value(__tact_context_get());
        throw_unless(28284, (($amountToSend - 30000000) > 0));
        $amountToSend = ($amountToSend - 30000000);
        $global_send($SendParameters$_constructor_to_value_body($childAddress, $amountToSend, $DepositToComp$_store_cell($DepositToComp$_constructor_id_sender($msg'id, __tact_context_get_sender()), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Request message
    if (op == 1390358458) {
        var $msg'id = in_msg~load_uint(256);
        var $msg'user = in_msg~__tact_load_address_opt();
        throw_unless(63522, (($Context$_get_value(__tact_context_get()) - 30000000) >= 0));
        slice $userAddress = $global_login(__tact_context_get_sender(), $global_myAddress(), null());
        var ($init'code, $init'data) = $CompensationPool$_init_child($msg'id, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 30000000, $Request$_store_cell($Request$_constructor_id_user($msg'id, $userAddress), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Approve message
    if (op == 1599388651) {
        var $msg'id = in_msg~load_int(257);
        var $msg'user = in_msg~__tact_load_address_opt();
        var $msg'sender = in_msg~__tact_load_address_opt();
        var $msg'balance = in_msg~load_varuint16();
        throw_unless(63522, (($Context$_get_value(__tact_context_get()) - 40000000) >= 0));
        throw_unless(7997, ( ~ __tact_slice_eq_bits_nullable_right(__tact_context_get_sender(), $msg'user) ));
        slice $userAddress = $global_login(__tact_not_null($msg'user), $global_myAddress(), null());
        var ($init'code, $init'data) = $CompensationPool$_init_child($msg'id, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $Update$_store_cell($Update$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $UpdateEarn$_store_cell($UpdateEarn$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 20000000, $Approve$_store_cell($Approve$_constructor_id_user_sender_balance($msg'id, $userAddress, __tact_context_get_sender(), 0), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Decline message
    if (op == 750137859) {
        var $msg'id = in_msg~load_int(257);
        var $msg'user = in_msg~__tact_load_address_opt();
        var $msg'sender = in_msg~__tact_load_address_opt();
        var $msg'balance = in_msg~load_varuint16();
        throw_unless(63522, (($Context$_get_value(__tact_context_get()) - 40000000) >= 0));
        throw_unless(10453, ( ~ __tact_slice_eq_bits_nullable_right(__tact_context_get_sender(), $msg'user) ));
        slice $userAddress = $global_login(__tact_context_get_sender(), $global_myAddress(), null());
        var ($init'code, $init'data) = $CompensationPool$_init_child($msg'id, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $Update$_store_cell($Update$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 10000000, $UpdateEarn$_store_cell($UpdateEarn$_constructor_sender(__tact_context_get_sender()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 20000000, $Decline$_store_cell($Decline$_constructor_id_user_sender_balance($msg'id, $userAddress, __tact_context_get_sender(), 0), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive Repay message
    if (op == 1754501411) {
        var $msg'id = in_msg~load_uint(256);
        slice $userAddress = $global_login(__tact_context_get_sender(), $global_myAddress(), null());
        $global_send($SendParameters$_constructor_to_value_mode_body($userAddress, 0, 64, $Repay$_store_cell($Repay$_constructor_id($msg'id), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_external(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_external(slice in_msg) impure {
    ;; Load contract data
    var ($self'nowId, $self'seqno, $self'serverPublicKey) = $AppContract$_contract_load();
    
    var op = in_msg~load_opcode_external();
    ;; Receive ExternalBorrow message
    if (op == 359929327) {
        var $msg'pubkey = in_msg~load_uint(256);
        var $msg'signature = in_msg~load_ref().begin_parse();
        var $msg'payload = in_msg~load_ref();
        $global_acceptMessage();
        int $pubkey = $msg'pubkey;
        slice $signature = $msg'signature;
        cell $payload = $msg'payload;
        int $hash = $Cell$_fun_hash($payload);
        throw_unless(48401, $global_checkSignature($hash, $signature, $pubkey));
        slice $userAddr = $global_getWalletAddress($pubkey);
        slice $slice = $Cell$_fun_beginParse($payload);
        int $idBorrow = $slice~load_uint(64);
        int $idComp = $slice~load_uint(64);
        int $amount = $slice~$Slice$_fun_loadCoins();
        slice $to = $slice~$Slice$_fun_loadAddress();
        int $fcc = $slice~$Slice$_fun_loadCoins();
        int $balanceComp = $slice~$Slice$_fun_loadCoins();
        int $balanceBorrow = $slice~$Slice$_fun_loadCoins();
        var ($initComp'code, $initComp'data) = $CompensationPool$_init_child($idComp, $global_myAddress());
        slice $compPoolAddress = $global_contractAddress(($initComp'code, $initComp'data));
        $global_send($SendParameters$_constructor_to_value_body($compPoolAddress, 20000000, $Borrow$_store_cell($Borrow$_constructor_idBorrow_idComp_amount_to_time_fcc_balanceComp_balanceBorrow($idBorrow, $idComp, $amount, $userAddr, 0, $fcc, $balanceComp, $balanceBorrow), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive ExternalRequest message
    if (op == 753189113) {
        var $msg'pubkey = in_msg~load_uint(256);
        var $msg'signature = in_msg~load_ref().begin_parse();
        var $msg'payload = in_msg~load_ref();
        $global_acceptMessage();
        int $pubkey = $msg'pubkey;
        slice $signature = $msg'signature;
        cell $payload = $msg'payload;
        int $hash = $Cell$_fun_hash($payload);
        throw_unless(48401, $global_checkSignature($hash, $signature, $pubkey));
        slice $userAddr = $global_getWalletAddress($pubkey);
        slice $slice = $Cell$_fun_beginParse($payload);
        int $id = $slice~load_uint(256);
        slice $userContract = $global_login($userAddr, $global_myAddress(), null());
        var ($init'code, $init'data) = $CompensationPool$_init_child($id, $global_myAddress());
        slice $childAddress = $global_contractAddress(($init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_body($childAddress, 30000000, $Request$_store_cell($Request$_constructor_id_user($id, $userContract), begin_cell())));
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Receive ExternalLogin message
    if (op == 2963964483) {
        var $msg'serverSignature = in_msg~load_ref().begin_parse();
        var $msg'payload = in_msg~load_ref();
        $global_acceptMessage();
        int $payloadHash = $Cell$_fun_hash($msg'payload);
        int $isServerValid = $global_checkSignature($payloadHash, $msg'serverSignature, $self'serverPublicKey);
        throw_unless(44371, $isServerValid);
        slice $ds = $Cell$_fun_beginParse($msg'payload);
        int $msgSeqno = $ds~load_uint(32);
        throw_unless(17654, ($msgSeqno == $self'seqno));
        int $userPublicKey = $ds~load_uint(256);
        slice $userSignature = $ds~load_bits(512);
        int $emptyCellHash = $Cell$_fun_hash($Builder$_fun_endCell($global_beginCell()));
        cell $expectedSignedCell = $Builder$_fun_endCell(store_uint(store_uint(store_uint($global_beginCell(), 65535, 16), 1684108385, 32), $emptyCellHash, 256));
        int $isUserValid = $global_checkSignature($Cell$_fun_hash($expectedSignedCell), $userSignature, $userPublicKey);
        throw_unless(38055, $isUserValid);
        $global_acceptMessage();
        slice $walletAddr = $global_getWalletAddress($userPublicKey);
        $self'seqno = ($self'seqno + 1);
        slice $userContract = $global_login($walletAddr, $global_myAddress(), 1);
        $AppContract$_contract_store(($self'nowId, $self'seqno, $self'serverPublicKey));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}

() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_external from the dict
                dup -1 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                DUP INC IFNOTJMP:<{
                    // place recv_external here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}